import styled, { Styled } from "styled-components";

const ReviewStock = () => {
  return (
    <>
      <div>
        <button className="btn">검색</button>
      </div>
    </>
  );
};
export default ReviewStock;
